// 세개의 Int a,b,c 가 주어졌을 때 두번째로 큰 수를 Int로 반환하는 함수

fun main() {   // 메인 함수가 리턴값을 가질수가 없다? 
    val a = 12  // 12가 출력이 되야함 // 조건문 + 함수 써서
    val b = 66
    val c = 3
	if (a<=b && a>=c){
        print(a)
    }
}
    
 

// 3개의 수를 모두 더하고 최댓값과 최솟값을 뺄때 두번째로 큰수 구하는 함수

// fun second (a: Int, b: Int, c: Int):Int{  //조건문 3개로 a가 2번째로 클때, b가 2번째로 클때, c가 두번째로 클때
//    if (a<=b,a>=c) 

// }

// [리스트 + 반복문]
// 전달받은 리스트의 전체 합 계산
// ex) listOf(100,80,90) -> 90

// fun main() {
    
// }
