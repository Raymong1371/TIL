
fun main() {
    second(listOf(5,3,7,1)) //출력값이 5가 나오게
}

fun second(input: List<Int>): Int{  //함수선언 매개변수
    //입력받은리스트에서 두번째로 큰 수를 반환하는 함수를 만들어야함
    
   //리스트 부터 입력하는게우선  리스트 1,3,5,7
   
    //입력받은 리스트에서 조건문 if 3보다 크고 7보다 작은 값을 출력하도록하기
   
      
    
    //if 와 for문 사용해서 어떻게 하지?
  

    
}



124,3,262,62,3112523,6347,346253,543,1243534,6457,34523,52352,43735
//숫자들을 먼저 쭉 보고 숫자들의 갯수를 먼저 파악

//갯수가 많으면 많을수록 큰수라는것을 인식함

//숫자갯수가 가장 많은 수 2개 중 맨 앞자리수를 비교하여 작은수를 출력

23,42,12,57,34,84,73,24,13,98,35,74,21,13
//숫자들의 맨 앞자리 한개의 숫자만 먼저 비교를 함 
//앞자리가 9인 숫자는 1개 가장 큰 수인것을 인식
//그다음 앞자리가 8인 숫자도 1개로 인식 8* 숫자 출력

232,23,324,421,135,235,321,325,23,42,12,57,123
// 나열된 숫자의 갯수가 많을수록 큰 수   
// 숫자의 갯수가 같다면 첫번째 숫자부터 차례대로 비교하여 출력 
// 출력된 값 중 두번째 값을 출력


val input = listOf(~~~~~~~~~~~)
// 가장 큰 수 찾는 방법
// 숫자의 갯수를 파악하는 방법에 대한 코드

//개수가 가장 많은 숫자를 찾는다.

//input 길이만큼 반복
for(number in input) {
    //numberLength에 number의 숫자 갯수를 저장
    val numberLength = number.toString().length
    //넘버랭쓰 1 이면 1자릿수, 2면 2자릿수, 3이면 3자릿수
      
    // 숫자 갯수가 많을수록 넘버랭쓰 반환값이 크다
    // 넘버랭쓰 반환값이 가장 큰 수만 선별
    // 넘버랭쓰 반환값이 같을시, 앞 자리 숫자가 큰 수부터 출력  
    // 만약 앞자리 숫자가 같을시, 다음번째 숫자를 비교하여 큰값부터 출력  
   
}

// // // 3자릿수 중 맨 앞자리가 가장 큰 수 





1,52,325,75,2,25,3447,236,53745,2,5236,236,36,26,3635,72,6326,16,534362
//왼쪽에서 오른쪽으로 값을 순서대로 읽으면서 크기를 비교 
//만약 왼쪽의 값보다 오른쪽의 값이 클때 값을 저장
//가장 큰 값을 저장 - 최댓값을 찾는 과정 
// 그다음 최솟값 찾 챗봇사용 ㄴ  
